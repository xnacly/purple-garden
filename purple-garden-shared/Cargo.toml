[package]
name = "purple-garden-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"], optional = true }

[features]
cli = ["dep:clap"]
trace = []
