# Embed-fn-dispatch

Showcases the ability to query the purple garden runtime for a function defined
in it by its name, then passing rust values into said function, thereby
executing it, and getting the return value as a rust value out after execution.
