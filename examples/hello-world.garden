# vim: filetype=python

std::println("Hello"+" "+"World")
# gc manual cycle: only the resulting "Hello World" should be considered alive,
# since its still in r1 after the above 
std::runtime::gc::cycle()
